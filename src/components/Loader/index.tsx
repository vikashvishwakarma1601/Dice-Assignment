import React from "react";
import { LoaderContainer } from "./style";

const Loader = () => {
  return (
    <LoaderContainer>
      <div>Loading...</div>
    </LoaderContainer>
  );
};

export default Loader;
